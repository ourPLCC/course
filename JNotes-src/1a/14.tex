\begin{minipage}[t]{\sw}
\slidenumber
\LARGE
{\bf PLCC -- Parsing (continued)}\exx
Here's a simple example grammar:
{\Large
\begin{qv}
<tree>:Leaf     ::= <NUMBER>
<tree>:Interior ::= LPAREN <SYMBOL> <tree>left <tree>right RPAREN
\end{qv}
}
The abstract \verb'Tree' class looks similar to this:
{\Large
\begin{qv}
public abstract class Tree {

    public static Tree parse(Scan scn$) {
        Token t$ = scn$.cur();
        Token.Match match$ = t$.match;
        switch(match$) {
        case NUM:
            return Leaf.parse(scn$);
        case LPAREN:
            return Interior.parse(scn$);
        default:
            throw new PLCCException(
                "Parse error",
                "Tree cannot begin with " + t$.errString()
            );
        }
    }
}
\end{qv}
}
\end{minipage}
\clearpage
